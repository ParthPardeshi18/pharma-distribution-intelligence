# ERP BI & Decision-Support System — Python dependencies
# Production runtime: Python 3.11 (see .python-version). The full scientific
# stack — matplotlib, Prophet, scikit-learn, statsmodels — installs natively on
# 3.11. Pinned loosely (>=); the committed venv pins exact versions.

# --- Core data stack (needed Phase 0-2) ---
pandas>=2.2
numpy>=1.26
openpyxl>=3.1          # .xlsx read/write
xlsxwriter>=3.2        # styled Excel exports
pyyaml>=6.0            # config files
SQLAlchemy>=2.0        # warehouse ORM/engine (SQLite)
python-dotenv>=1.0     # .env loading

# --- Reporting / documents (Phase 5-7) ---
python-docx>=1.1       # .docx business reports

# --- Visualisation (Phase 3-4) ---
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20

# --- Analytics / ML (Phase 4) ---
scikit-learn>=1.4
statsmodels>=0.14      # ARIMA fallback for forecasting
prophet>=1.1           # forecasting with confidence intervals (works natively on 3.11)

# --- Geospatial / GIS (Phase 6.6) ---
shapely>=2.0           # geometry ops; Voronoi tessellation, polygon dissolve
scipy>=1.11            # spatial algorithms

# --- Application UI (Phase v1.1 — primary user interface) ---
streamlit>=1.58        # multi-page app: auth, caching, drill-down, downloads
pydeck>=0.9            # native GeoJSON maps (deck.gl) for the geographic views

# --- Testing / quality ---
pytest>=8.0
